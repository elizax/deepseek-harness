diff --git a/dist/api/anthropic-messages.js b/dist/api/anthropic-messages.js
index 70ead54dbbbea64daff7350b70ce1c23352cd208..720e816dbc79ae80c5ce99ccc7d9e1e42e8ad5e0 100644
--- a/dist/api/anthropic-messages.js
+++ b/dist/api/anthropic-messages.js
@@ -11,6 +11,7 @@ import { resolveJsonSchemaStrictSampling } from "./constrained-sampling.js";
 import { buildCopilotDynamicHeaders, hasCopilotVisionInput } from "./github-copilot-headers.js";
 import { adjustMaxTokensForThinking, buildBaseOptions, clampMaxTokensToContext } from "./simple-options.js";
 import { transformMessages } from "./transform-messages.js";
+import { createProxyFetch, resolveExplicitProxyUrl } from "../utils/node-http-proxy.js";
 /**
  * Resolve cache retention preference.
  * Defaults to "short" and uses PI_CACHE_RETENTION for backward compatibility.
@@ -356,7 +357,7 @@ export const stream = (model, context, options) => {
                 }
                 const cacheRetention = resolveCacheRetention(options?.cacheRetention, options?.env);
                 const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
-                const created = createClient(model, apiKey, options?.interleavedThinking ?? true, shouldUseFineGrainedToolStreamingBeta(model, context), options?.headers, copilotDynamicHeaders, cacheSessionId);
+                const created = createClient(model, apiKey, options?.interleavedThinking ?? true, shouldUseFineGrainedToolStreamingBeta(model, context), options?.headers, copilotDynamicHeaders, cacheSessionId, options?.env);
                 client = created.client;
                 isOAuth = created.isOAuthToken;
             }
@@ -636,7 +637,7 @@ export const streamSimple = (model, context, options) => {
 function isOAuthToken(apiKey) {
     return apiKey.includes("sk-ant-oat");
 }
-function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStreamingBeta, optionsHeaders, dynamicHeaders, sessionId) {
+function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStreamingBeta, optionsHeaders, dynamicHeaders, sessionId, optionsEnv) {
     // Adaptive thinking models have interleaved thinking built in, so skip the beta header.
     const needsInterleavedBeta = interleavedThinking && model.compat?.forceAdaptiveThinking !== true;
     const betaFeatures = [];
@@ -646,6 +647,8 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
     if (needsInterleavedBeta) {
         betaFeatures.push(INTERLEAVED_THINKING_BETA);
     }
+    const proxyUrl = resolveExplicitProxyUrl(optionsEnv);
+    const proxyFetch = proxyUrl === undefined ? undefined : createProxyFetch(proxyUrl);
     // Copilot: Bearer auth, selective betas.
     if (model.provider === "github-copilot") {
         const client = new Anthropic({
@@ -658,6 +661,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
                 "anthropic-dangerous-direct-browser-access": "true",
                 ...(betaFeatures.length > 0 ? { "anthropic-beta": betaFeatures.join(",") } : {}),
             }, model.headers, dynamicHeaders, optionsHeaders),
+            ...(proxyFetch === undefined ? {} : { fetch: proxyFetch }),
         });
         return { client, isOAuthToken: false };
     }
@@ -675,6 +679,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
                 "user-agent": `claude-cli/${claudeCodeVersion}`,
                 "x-app": "cli",
             }, model.headers, optionsHeaders),
+            ...(proxyFetch === undefined ? {} : { fetch: proxyFetch }),
         });
         return { client, isOAuthToken: true };
     }
@@ -691,6 +696,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
         baseURL: model.baseUrl,
         dangerouslyAllowBrowser: true,
         defaultHeaders,
+        ...(proxyFetch === undefined ? {} : { fetch: proxyFetch }),
     });
     return { client, isOAuthToken: false };
 }
diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index ed81baa3860fa20cf9d38885f9dd4b6b2f639045..424cdc0b07d9952b76c497e82128195b7fd439a5 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -13,6 +13,7 @@ import { buildCopilotDynamicHeaders, hasCopilotVisionInput } from "./github-copi
 import { clampOpenAIPromptCacheKey } from "./openai-prompt-cache.js";
 import { buildBaseOptions } from "./simple-options.js";
 import { transformMessages } from "./transform-messages.js";
+import { createProxyFetch, resolveExplicitProxyUrl } from "../utils/node-http-proxy.js";
 /**
  * Check if conversation messages contain tool calls or tool results.
  * This is needed because Anthropic (via proxy) requires the tools param
@@ -125,7 +126,7 @@ export const stream = (model, context, options) => {
             const grammarToolInputProperties = createGrammarToolInputProperties(context.tools, compat.supportsOpenAIGrammarTools);
             const cacheRetention = resolveCacheRetention(options?.cacheRetention, options?.env);
             const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
-            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId, compat);
+            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId, compat, options?.env);
             let params = buildParams(model, context, options, compat, cacheRetention, grammarToolInputProperties);
             const nextParams = await options?.onPayload?.(params, model);
             if (nextParams !== undefined) {
@@ -476,7 +477,7 @@ export const streamSimple = (model, context, options) => {
         toolChoice,
     });
 };
-function createClient(model, context, apiKey, optionsHeaders, sessionId, compat = getCompat(model)) {
+function createClient(model, context, apiKey, optionsHeaders, sessionId, compat = getCompat(model), optionsEnv) {
     const headers = { ...model.headers };
     if (model.provider === "github-copilot") {
         const hasImages = hasCopilotVisionInput(context.messages);
@@ -502,11 +503,14 @@ function createClient(model, context, apiKey, optionsHeaders, sessionId, compat
     if (optionsHeaders) {
         Object.assign(headers, optionsHeaders);
     }
+    const proxyUrl = resolveExplicitProxyUrl(optionsEnv);
+    const proxyFetch = proxyUrl === undefined ? undefined : createProxyFetch(proxyUrl);
     return new OpenAI({
         apiKey,
         baseURL: model.baseUrl,
         dangerouslyAllowBrowser: true,
         defaultHeaders: headers,
+        ...(proxyFetch === undefined ? {} : { fetch: proxyFetch }),
     });
 }
 function buildParams(model, context, options, compat = getCompat(model), cacheRetention = resolveCacheRetention(options?.cacheRetention, options?.env), grammarToolInputProperties = createGrammarToolInputProperties(context.tools, compat.supportsOpenAIGrammarTools)) {
diff --git a/dist/index.js b/dist/index.js
index 2f1880e3cff9fbef25faa54b915936b3e7a154df..efe1f5fea49c5accdb0b73666a2240dc1f32e0bf 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -11,6 +11,7 @@ export * from "./providers/faux.js";
 export * from "./session-resources.js";
 export * from "./types.js";
 export * from "./utils/diagnostics.js";
+export { createProxyFetch } from "./utils/node-http-proxy.js";
 export * from "./utils/event-stream.js";
 export * from "./utils/json-parse.js";
 export * from "./utils/overflow.js";
diff --git a/dist/utils/node-http-proxy.js b/dist/utils/node-http-proxy.js
index c3b44bd73d3178f8fbf5d1cbdf46ca217610497f..6887ec3ce574a80a7fe6f694c0b36a0457e398fe 100644
--- a/dist/utils/node-http-proxy.js
+++ b/dist/utils/node-http-proxy.js
@@ -1,4 +1,9 @@
 import { getProviderEnvValue } from "./provider-env.js";
+import { HttpProxyAgent } from "http-proxy-agent";
+import { HttpsProxyAgent } from "https-proxy-agent";
+import { request as httpRequest } from "node:http";
+import { request as httpsRequest } from "node:https";
+import { Readable } from "node:stream";
 const DEFAULT_PROXY_PORTS = {
     ftp: 21,
     gopher: 70,
@@ -89,4 +94,85 @@ export function resolveHttpProxyUrlForTarget(targetUrl, env) {
     }
     return proxyUrl;
 }
+/**
+ * Resolve an explicitly configured proxy URL from provider-scoped environment
+ * values only (never process.env). A proxy named here is authoritative: it is
+ * used for every request on the route regardless of no_proxy, because the
+ * deployment opted that route into the proxy on purpose.
+ * @param env - the provider-scoped environment values passed through stream options.
+ * @returns the parsed http(s) proxy URL, or undefined when none is configured.
+ */
+export function resolveExplicitProxyUrl(env) {
+    if (!env) {
+        return undefined;
+    }
+    const raw = env["HTTP_PROXY"] || env["http_proxy"] || env["HTTPS_PROXY"] || env["https_proxy"] || env["ALL_PROXY"] || env["all_proxy"];
+    if (!raw) {
+        return undefined;
+    }
+    try {
+        const url = new URL(raw);
+        if (url.protocol === "http:" || url.protocol === "https:") {
+            return url;
+        }
+    }
+    catch {
+        // Fall through to undefined: an unparsable proxy is a configuration error
+        // surfaced by the transport (the request simply goes direct).
+    }
+    return undefined;
+}
+/**
+ * Build a `fetch` implementation that routes every request through the given
+ * HTTP(S) proxy. Used to give SDK-backed providers (Anthropic/OpenAI) proxy
+ * support without a global dispatcher: the SDKs accept a custom `fetch`.
+ * @param proxyUrl - the parsed proxy URL.
+ * @returns a fetch-compatible function.
+ */
+export function createProxyFetch(proxyUrl) {
+    const httpAgent = new HttpProxyAgent(proxyUrl);
+    const httpsAgent = new HttpsProxyAgent(proxyUrl);
+    return async (input, init = {}) => {
+        const url = typeof input === "string" ? new URL(input) : input;
+        const isHttps = url.protocol === "https:";
+        const reqFn = isHttps ? httpsRequest : httpRequest;
+        return await new Promise((resolve, reject) => {
+            const req = reqFn({
+                protocol: url.protocol,
+                hostname: url.hostname,
+                port: url.port ? Number(url.port) : (isHttps ? 443 : 80),
+                path: url.pathname + url.search,
+                method: init.method ?? "GET",
+                headers: init.headers,
+                agent: isHttps ? httpsAgent : httpAgent,
+                signal: init.signal,
+            }, (res) => {
+                resolve(new Response(Readable.toWeb(res), {
+                    status: res.statusCode,
+                    statusText: res.statusMessage,
+                    headers: res.headers,
+                }));
+            });
+            req.on("error", reject);
+            const body = init.body;
+            if (body === undefined || body === null) {
+                req.end();
+                return;
+            }
+            if (typeof body === "string" || Buffer.isBuffer(body)) {
+                req.end(body);
+                return;
+            }
+            if (typeof body.pipe === "function") {
+                body.pipe(req);
+                return;
+            }
+            if (typeof body.getReader === "function") {
+                Readable.fromWeb(body).pipe(req);
+                return;
+            }
+            req.end(String(body));
+        });
+    };
+}
 //# sourceMappingURL=node-http-proxy.js.map
